```mermaid
graph TD;
    DataSources((Data Sources)) --> WeChoseThis("Why we chose this")
    WeChoseThis --> DataCollected((Data Collected))
    DataCollected --> DataProcessing((Data Processing))
    DataProcessing --> DataManipulation((Data Manipulation))
```
